{
  "name": "visual-learner",
  "version": "0.1.0",
  "description": "Interactive visual learning from PDFs — powered by AI",
  "private": true,
  "workspaces": [
    "frontend",
    "electron"
  ],
  "scripts": {
    "dev": "node scripts/start-dev.mjs",
    "dev:vite": "cd frontend && npx vite",
    "dev:electron": "cd electron && npx vite -c ../electron.vite.config.ts",
    "build": "npm run build:frontend && npm run build:electron",
    "build:frontend": "cd frontend && npx vite build",
    "build:electron": "node scripts/build-electron.mjs && npx electron-builder",
    "build:backend": "cd backend && pyinstaller main.py --name visual-learner-backend --distpath dist",
    "test": "npm run test:frontend && npm run test:backend",
    "test:frontend": "cd frontend && vitest run",
    "test:backend": "cd backend && pytest",
    "test:coverage": "cd frontend && vitest run --coverage && cd ../backend && pytest --cov",
    "lint": "npm run lint:frontend && npm run lint:backend",
    "lint:frontend": "cd frontend && npx vue-tsc --noEmit && npx eslint src/**/*.{ts,vue} && npx prettier --check src/**/*.{ts,vue,css,json}",
    "lint:backend": "cd backend && npx black --check . && npx isort --check . && npx mypy app/",
    "format": "npm run format:frontend && npm run format:backend",
    "format:frontend": "cd frontend && npx prettier --write src/**/*.{ts,vue,css,json}",
    "format:backend": "cd backend && npx black . && npx isort .",
    "clean": "rm -rf frontend/dist dist-electron backend/dist",
    "clean:all": "rm -rf node_modules frontend/node_modules backend/__pycache__ dist-electron frontend/dist backend/dist",
    "install:all": "npm install && cd backend && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "8.2.2",
    "cross-env": "7.0.3",
    "electron": "29.3.0",
    "electron-builder": "24.13.3",
    "rimraf": "5.0.5",
    "vite-plugin-electron": "0.28.4",
    "vite-plugin-electron-renderer": "0.14.5"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
